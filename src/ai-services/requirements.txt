fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-multipart>=0.0.17
httpx>=0.27.0
pytest>=8.3.0
psycopg[binary]>=3.2.0
# Connection pooling for the AI Services Postgres access path.
psycopg_pool>=3.2,<4
minio>=7.2.0
# Default semantic embedding backend: multilingual-e5-base (768-dim).
# CPU torch keeps the image lean; pinned for reproducibility. For a smaller
# image use the CPU wheel index: pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==3.2.1
torch==2.4.1
# Document parsing
pypdf>=5.1.0
python-docx>=1.1.2
# Phase 2 training
scikit-learn>=1.5.0
# Optional LightGBM backend
# lightgbm>=4.5.0
# Optional: LangChain/LlamaIndex later
# langchain>=0.3.0
# llama-index>=0.12.0
